# CODEX_QA_TASK report (runtime sanity)

## Scope & branch
- Requested: validate PR #101 (`heuristic texture classifier`) on real `titanwave-sonic` tracks.
- Environment limitation: repository has no configured git remotes, so `git fetch origin pull/101/head:pr-101` was not possible.
- Executed against current local branch: `work`.

## Environment
- OS: Linux (container)
- Python: 3.12.12
- Executable: `/root/.pyenv/versions/3.12.12/bin/python`
- Working directory: `/workspace/factory-vm`
- `FACTORY_PY_DEPS_DIR` set to absolute path: `/workspace/factory-vm/data/pydeps`

## Process startup commands used
- API:
  - `FACTORY_BASIC_AUTH_USER=<set via env> FACTORY_BASIC_AUTH_PASS=<set via env> FACTORY_PY_DEPS_DIR=/workspace/factory-vm/data/pydeps /root/.pyenv/versions/3.12.12/bin/python -m services.factory_api`
- Worker (`track_jobs`):
  - `FACTORY_BASIC_AUTH_USER=<set via env> FACTORY_BASIC_AUTH_PASS=<set via env> FACTORY_PY_DEPS_DIR=/workspace/factory-vm/data/pydeps /root/.pyenv/versions/3.12.12/bin/python -m services.workers --role track_jobs`

## Runtime results
- `/v1/admin/yamnet/status` returned:
  - `installed=false`
  - `import_tf=false`, `import_hub=false`
  - error: `numpy.core.umath failed to import`
- Analyze launch for requested channel returned:
  - HTTP payload: `{"detail":"channel not found"}`
- DB summary extraction:
  - table found: `track_features`
  - schema differs (`track_pk`, `payload_json`, `computed_at`)
  - no analyzable samples for `titanwave-sonic` (`SAMPLES: 0`)

## PASS/FAIL against criteria
- **FAIL** (blocked by runtime prerequisites):
  - could not run ANALYZE_TRACKS on `titanwave-sonic` due missing channel in local DB;
  - could not validate heuristic texture distribution on 20 real tracks;
  - YamNet import path is currently broken in this runtime due NumPy ABI mismatch.

## Artifact files
- `.qa_api.log`
- `.qa_worker_track_jobs.log`
- `.qa_yamnet_status.json`
- `.qa_analyze_launch.json`
- `.qa_worker_tail.txt`
- `.qa_texture_summary.txt`
- tails:
  - `.qa_api_tail200.txt`
  - `.qa_worker_tail400.txt`
