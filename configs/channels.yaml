# Seed-only config for channels + optional per-channel YouTube credential path overrides (paths only, no secret content):
# - yt_token_json_path: path to OAuth token JSON for this channel
# - yt_client_secret_json_path: optional path to client secret JSON for this channel
# Runtime reads channels from DB. Re-run scripts/seed_configs.py after editing this file.

channels:
  - slug: "darkwood-reverie"
    display_name: "Darkwood Reverie"
    kind: "LONG"
    weight: 1.0
    render_profile: "long_1080p24"
    autopublish_enabled: false
    yt_token_json_path: "/secure/youtube/channels/darkwood-reverie/token.json"
    yt_client_secret_json_path: "/secure/youtube/client_secret.json"

  - slug: "channel-b"
    display_name: "Channel B"
    kind: "LONG"
    weight: 1.0
    render_profile: "long_1080p24"
    autopublish_enabled: false

  - slug: "channel-c"
    display_name: "Channel C"
    kind: "LONG"
    weight: 1.0
    render_profile: "long_1080p24"
    autopublish_enabled: false

  - slug: "channel-d"
    display_name: "Channel D"
    kind: "LONG"
    weight: 1.0
    render_profile: "long_1080p24"
    autopublish_enabled: false

  - slug: "titanwave-sonic"
    display_name: "TitanWave Sonic"
    kind: "TITANWAVE"
    weight: 0.0
    render_profile: "titanwave_1080p24"
    autopublish_enabled: false
